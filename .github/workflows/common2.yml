name: workflow_2
on:
  workflow_run:
    # branches: [test]
    workflows: ['workflow_3']
    types:
      - completed
jobs:
  Second_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo workflow 2
