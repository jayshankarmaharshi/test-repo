name: workflow_call_stag
on: workflow_call

#--------------------------------------------------------- UPDATE MANIFEST FILES WITH COMMIT HASHES -----------------------------------------------------------------

jobs:
  reusable_workflow_job_stag:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - run: echo you are in call workflow function STAGING
